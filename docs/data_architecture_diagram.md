# Data architecture and functions

Interactive view of the data architecture: the files produced by BERTrend (`bertrend.dill` and the other artefacts),
the BERTrend functions and the relational schema, with click-to-expand details for every data structure.

<a class="architecture-open" href="data_architecture.html" target="_blank" rel="noopener">Open the diagram in a new tab ↗</a>

```{raw} html
<iframe class="architecture-frame"
        src="data_architecture.html"
        title="BERTrend architecture and functions"
        loading="lazy"></iframe>
```

:::{tip}
The diagram is a self-contained HTML page (`data_architecture.html`): it embeds its own styles and needs no
JavaScript, so it is copied verbatim to the documentation site and can also be downloaded and opened directly in a
browser. GitHub does not render HTML files, which is why the source repository only links to it.
:::
